import torch

tensor = torch.arange(1, 13).reshape(1, 3, 4)
print(tensor)

tensor2 = torch.arange(1, 10).reshape(1, 3, 3)
print(tensor2)
print(tensor2.shape)
print(tensor2[0][0][0])
print(tensor2[:, :, 0])
